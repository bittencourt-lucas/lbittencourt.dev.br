---
interface Props {
  class?: string;
  as?: string;
}

const { class: className = "", as: Tag = "span" } = Astro.props;
---

<Tag class:list={["font-mono text-xs tracking-widest uppercase text-fg-muted", className]}>
  <slot />
</Tag>
