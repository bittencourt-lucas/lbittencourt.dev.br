---
interface Props {
  name: string;
  label?: string;
  placeholder?: string;
  required?: boolean;
  rows?: number;
  error?: string;
  class?: string;
}

const {
  name,
  label,
  placeholder,
  required = false,
  rows = 5,
  error,
  class: className = "",
} = Astro.props;
---

<div class:list={["flex flex-col gap-1.5", className]}>
  {
    label && (
      <label for={name} class="font-mono text-xs tracking-widest uppercase text-fg-muted">
        {label}
        {required && <span class="text-accent ml-1" aria-hidden="true">*</span>}
      </label>
    )
  }
  <textarea
    id={name}
    name={name}
    placeholder={placeholder}
    required={required}
    rows={rows}
    aria-invalid={error ? "true" : undefined}
    aria-describedby={error ? `${name}-error` : undefined}
    class:list={[
      "w-full bg-bg border text-fg font-sans text-sm px-4 py-3 resize-y",
      "placeholder:text-fg-muted/50",
      "focus:outline-none focus:border-accent",
      "transition-colors duration-150",
      error ? "border-red-500" : "border-border",
    ]}
  ></textarea>
  {
    error && (
      <p id={`${name}-error`} class="font-mono text-xs text-red-500" role="alert">
        {error}
      </p>
    )
  }
</div>
