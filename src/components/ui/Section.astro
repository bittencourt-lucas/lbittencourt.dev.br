---
interface Props {
  id?: string;
  class?: string;
  as?: string;
}

const { id, class: className = "", as: Tag = "section" } = Astro.props;
---

<Tag id={id} class:list={["py-24 md:py-32", className]}>
  <slot />
</Tag>
